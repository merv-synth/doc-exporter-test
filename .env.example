# Comma-separated origins allowed by backend CORS policy.
# For local docker-compose, keep frontend origin.
ALLOWED_ORIGINS=http://localhost:5500

# Optional overrides
# REQUEST_TIMEOUT=30
# SYNTHESIA_API_BASE=https://api.synthesia.io/v2

# Frontend runtime config
# Set to your deployed backend URL when serving frontend from GitHub Pages.
API_BASE=http://localhost:8000
